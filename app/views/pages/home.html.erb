<% if current_user %>
<div>
	<%= render partial: "posts/form", locals: {post: @new_post} %>
</div>

<div>
	<% @posts.each do |post| %>
	<%= render partial: "posts/post", locals: {post: post} %>
	<% end %>
</div>
<% else %>
<div>
	<% @posts.each do |post| %>
	<%= render partial: "posts/post", locals: {post: post} %>
	<% end %>
</div>
<% end %>
